apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: baw-services
  namespace: {{ .Values.namespace }}
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      monitored: "true"
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 30s