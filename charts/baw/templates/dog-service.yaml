apiVersion: apps/v1
kind: Deployment
metadata:
  name: dog-service
  namespace: {{ .Values.namespace }}
spec:
  replicas: {{ .Values.dogService.replicas }}
  selector:
    matchLabels:
      app: dog-service
  template:
    metadata:
      labels:
        app: dog-service
    spec:
      imagePullSecrets:
        - name: {{ .Values.imagePullSecret }}
      containers:
        - name: dog-service
          image: {{ .Values.dogService.image }}:{{ .Values.dogService.tag }}
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: {{ .Values.dogService.port }}
          resources:
            requests:
              memory: {{ .Values.dogService.resources.requests.memory | quote }}
              cpu: {{ .Values.dogService.resources.requests.cpu | quote }}
            limits:
              memory: {{ .Values.dogService.resources.limits.memory | quote }}
              cpu: {{ .Values.dogService.resources.limits.cpu | quote }}
          env:
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://postgres:5432/dog_service_db"
            - name: SPRING_DATASOURCE_USERNAME
              value: "dog_svc_user"
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgresql-secret
                  key: DOG_SVC_DB_PASSWORD
            - name: SPRING_DATA_REDIS_HOST
              value: "redis"
            - name: SPRING_DATA_REDIS_PORT
              value: "6379"
            - name: SEED_OWNER_IDS
              valueFrom:
                secretKeyRef:
                  name: dog-service-secret
                  key: SEED_OWNER_IDS
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: {{ .Values.dogService.port }}
            initialDelaySeconds: {{ .Values.probes.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.periodSeconds }}
            timeoutSeconds: {{ .Values.probes.timeoutSeconds }}
            failureThreshold: {{ .Values.probes.failureThreshold }}
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: {{ .Values.dogService.port }}
            initialDelaySeconds: {{ .Values.probes.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.periodSeconds }}
            timeoutSeconds: {{ .Values.probes.timeoutSeconds }}
            failureThreshold: {{ .Values.probes.failureThreshold }}
          startupProbe:
            httpGet:
              path: /actuator/health
              port: {{ .Values.dogService.port }}
            failureThreshold: {{ .Values.probes.startup.failureThreshold }}
            periodSeconds: {{ .Values.probes.startup.periodSeconds }}
          securityContext:
            allowPrivilegeEscalation: {{ .Values.securityContext.allowPrivilegeEscalation }}
            runAsNonRoot: {{ .Values.securityContext.runAsNonRoot }}
            runAsUser: {{ .Values.securityContext.runAsUser }}
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: {{ .Values.securityContext.readOnlyRootFilesystem }}
          volumeMounts:
            - mountPath: /tmp
              name: tmp-volume
      volumes:
        - name: tmp-volume
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: dog-service
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: dog-service
  ports:
    - port: {{ .Values.dogService.port }}
      targetPort: {{ .Values.dogService.port }}